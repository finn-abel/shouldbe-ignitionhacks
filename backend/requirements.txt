# --- core service ---
fastapi
uvicorn[standard]
pydantic
python-dotenv

# --- persistence (step 1) ---
sqlalchemy

# --- .ics parsing (step 4 / Door C) ---
ics

# --- tests ---
pytest
httpx          # FastAPI TestClient dependency

# --- auth (step 11) ---
authlib               # Google OAuth (openid email profile scopes only)
itsdangerous          # signs the session cookie

# --- LLM scoring (step 12) ---
anthropic             # provider SDK; only used when SHOULDBE_USE_STUB is off

# --- NOT YET USED — added in the step that needs them ---
# postmarker            # Postmark in/outbound    (step 13)
